load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "main",
    srcs = ["main.rs"],
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
        "//src/utils:utils",
        "//src/http:http",
        "//src/db:db",
    ],
)
